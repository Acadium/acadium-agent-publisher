# Copy to .env and set strong values, e.g.: openssl rand -hex 24
DB_PASSWORD=change-me
DB_ROOT_PASSWORD=change-me-too

# WordPress is published on WP_BIND:WP_PORT for your reverse proxy.
WP_BIND=127.0.0.1
WP_PORT=8080
